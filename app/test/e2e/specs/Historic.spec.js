import utils from '../utils'

/* describe('Historic data page', function () {
  before(utils.beforeWithLogin)
  after(utils.after)

  it('Historic stock search works', async function () {
    await this.app.client.click('.md-tab-label=HISTORIC DATA')
    await this.app.client.setValue('[name="search"]', 'AAPL')
    await this.app.client.setValue('[name="dates"]', '01/05/2020 - 22/05/2020')
    // await this.app.client.setValue('[name="timeframe"]', '1D')
    await this.app.client.click('.md-button-content=Search')
    assert.isTrue(await this.app.client.isExisting('#graph'))
  })
}) */
